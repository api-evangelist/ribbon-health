generated: '2026-08-14'
method: searched
source: https://h1.com/request-demo/
docs: https://ribbon.readme.io/docs/ribbon-products
verified: '2026-08-14'
plan_count: 1
self_serve_pricing_published: false
pricing_probe:
  - url: https://h1.co/request-demo/
    status: 200
    checked: '2026-08-14'
    note: >-
      A "request a demo" form. No prices, no tiers, no published quota or overage anywhere on the
      H1 site. h1.co now 301s to h1.com; the demo form is the only pricing surface.
packaging_published: true
packaging_note: >-
  H1 does publish its PACKAGE STRUCTURE even though it publishes no prices. The docs describe
  three tiers of access — "Base API Access", "Upgraded API", and separately purchasable "Add-Ons"
  — with a feature matrix naming which modules fall in each (Providers, Locations, Networks,
  Specialties/Location Types and Focus Areas in Base; Custom/Boost Filters and Provider
  Cost/Quality in Upgraded; Eligibility Check and Price Transparency as add-ons). Entitlements are
  enforced at runtime as HTTP 403 and appear in the docs as named flags such as
  `doctors.can_price_transparency`. Source: https://ribbon.readme.io/docs/ribbon-products
  (page last updated 2026-05-14).
specificationVersion: "0.1"
id: ribbon-health-plans-pricing
name: Ribbon Health Plans and Pricing
description: >
  Ribbon Health (H1 API) does not publish self-serve pricing tiers publicly.
  Access to the API is enterprise-only and requires a demo/sales engagement.
  Pricing is customized based on data volume, endpoint access, and use case.
plans:
  - name: Enterprise (Custom)
    description: >
      Custom enterprise pricing for health plans, providers, and digital health
      companies. Includes access to provider search, location data, insurance
      network verification, eligibility, and price transparency endpoints.
      Pricing is negotiated based on API call volume, data scope, and contract
      terms. Request a demo at https://h1.co/request-demo/ to obtain a quote.
    url: https://h1.co/request-demo/
    features:
      - Provider search and NPI lookup
      - Location search and management
      - Insurance network verification
      - Member eligibility checking (powered by pVerify)
      - Procedure cost estimation (price transparency)
      - Clinical areas, conditions, and treatments data
      - Organization and network data
      - Custom provider directory management
      - Custom filters for providers and locations
      - Bearer token authentication
      - API v2.2 access
    rateLimits:
      - endpoint: v1/custom/providers
        limit: 1000
        period: minute
      - endpoint: v1/custom/providers/{npi}
        limit: 5000
        period: minute
      - endpoint: v1/custom/locations
        limit: 1000
        period: minute
      - endpoint: v1/custom/locations/{uuid}
        limit: 5000
        period: minute
      - endpoint: v1/deductible
        limit: 1000
        period: hour
